# 源数据库配置
SOURCE_SERVER=localhost
SOURCE_DATABASE=Database1
SOURCE_AUTH_TYPE=sql
SOURCE_USERNAME=sa
SOURCE_PASSWORD=your_password

# 目标数据库配置
TARGET_SERVER=localhost
TARGET_DATABASE=Database2
TARGET_AUTH_TYPE=windows

# 输出配置
OUTPUT_DIRECTORY=./reports
DEFAULT_FORMAT=html

# Web服务器配置
# 报告文件存储目录 (用于Web界面显示报告链接)
# Windows 示例: REPORTS_DIR=N:\备份\sqlserverdatabasecompare\reports  
# Linux 示例: REPORTS_DIR=/home/user/reports
# 如果不设置，默认使用项目根目录下的 reports 目录
REPORTS_DIR=

# 日志配置
LOG_LEVEL=info